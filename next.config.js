/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdn.freebiesupply.com'], // Add other domains if needed
  },
}

module.exports = nextConfig
